git_protocol: https
editor: /usr/bin/nvim
prompt: enabled
pager: less
aliases:
    co: pr checkout
    rv: repo view
    rcpub: repo create --private --source=. remote=origin
    rcpri: repo create --private --source=. remote=origin
http_unix_socket:
browser:
version: "1"
